version: 2.1
orbs:
  cypress: cypress-io/cypress@1.7.0

workflows:
  build:
    jobs:
      - cypress/run:
          command: npm run report #Tests run here
          store_artifacts: true
          post-steps:
            - store_artifacts:
                path: mochawesome-report
            - store_test_results:
                path: cypress/results

  nightly:
    triggers: #use the triggers key to indicate a scheduled build
      - schedule:
          cron: "0 0 * * *" # use cron syntax to set the schedule
          filters:
            branches:
              only:
                - add-cypress
    jobs:
      - cypress/run:
          command: npm run report #Tests run here
          store_artifacts: true
          post-steps:
            - store_test_results:
                path: cypress/reports
            - store_artifacts:
                path: mochareports
            - store_artifacts:
                path: mochawesome-report
            - store_test_results:
<<<<<<< HEAD
                path: cypress/results
=======
                path: cypress/results
>>>>>>> 44231a47a47477cf00d617b1606f8ea625d1ad05
